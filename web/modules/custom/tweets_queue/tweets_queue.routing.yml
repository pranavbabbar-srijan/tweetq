# Test API endpoints

tweets_queue.settings:
  path: '/admin/structure/tweets_queue/settings'
  defaults:
    _form: '\Drupal\tweets_queue\Form\TweetsQueueSettingsForm'
    _title: 'Tweeter Api configuration'
  requirements:
    _permission: 'administer site configuration'

tweets_queue.send_tweets:
  path: '/admin/structure/tweets_queue/send_tweets'
  defaults:
    _form: '\Drupal\tweets_queue\Form\SendTweetsForm'
    _title: 'Send Tweets'
  requirements:
    _permission: 'administer site configuration'

tweets_queue.file_upload:
  path: '/tweets-queue/upload-file'
  defaults:
    _form: '\Drupal\tweets_queue\Form\TweetsQueueCsvUploadForm'
    _title: 'Form test'
  requirements:
    _access: 'TRUE'